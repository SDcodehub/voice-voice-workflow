apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: voice-workflow

resources:
  - namespace.yaml
  - redis.yaml
  - riva-server.yaml
  - llm-server.yaml
  - asr-service.yaml
  - llm-service.yaml
  - tts-service.yaml
  - voice-gateway.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: voice-workflow
  app.kubernetes.io/version: "1.0.0"

